trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: sh gradlew --no-daemon assemble
  displayName: 'Run Gradle build'
- task: PublishBuildArtifacts@1
  displayName: 'Publish artifacts'
  inputs:
    pathtoPublish: build/libs/
    artifactName: lib
